import logo from "../images/logo.svg";

export default function Hero(){
    return(<div className="hero">
        Hero . . .
        <header className="hero-header flex justify-center">
        <img src={logo} className="App-logo" alt="logo" />

      </header>
      <section>

      </section>
    </div>);
}